import { StyleSheet } from 'react-native';
import * as colors from "../../../utils/colors";

const styles = StyleSheet.create({
    container: {
        // flex: 1
    },
    inputStyle: {
        fontSize:15,
        borderBottomWidth: 1,
        borderBottomColor: '#878C93',
        height: 30,
        padding: 0,
        flex: 0.5

    }
});
export default styles;